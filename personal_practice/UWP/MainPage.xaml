<Page
    x:Class="UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	
	<Grid>
		<Grid Padding="20">
			<Button x:Name="user" Content="Find User" Click="FindUserButton" />
			<Button x:Name="computer" Content="Find Computer" Click="ComputerSearchClick" />
			<TextBlock x:Name="messageBox" />
		</Grid>
	</Grid>
	<ListView x:Name="records">
		<ListView.ItemTemplate>
			<DataTemplate>
				<StackPanel Orientation="Horizontal" Spacing="20">
					<TextBlock Text="{Binding id}" Name="id" />
					<TextBlock Text="{Binding name}" Name="name" />
					<TextBlock Text="{Binding department}" Name="department"  />
				</StackPanel>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</Page>
